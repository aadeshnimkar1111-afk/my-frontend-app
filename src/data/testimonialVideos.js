// Placeholder slots - admin will attach each customer's name and YouTube
// video link here once we have footage of the families we've sold homes to.
export const TESTIMONIAL_VIDEOS = Array.from({ length: 20 }, (_, i) => ({
  id: i + 1,
  personName: null,
  videoId: null,
}))
